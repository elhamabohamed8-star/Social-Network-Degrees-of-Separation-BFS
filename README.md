# Social-Network-Degrees-of-Separation-BFS
A Python implementation of a Social Network Degrees of Separation system using graph theory and Breadth-First Search (BFS). The project loads friendship data from a CSV file, finds the shortest path between users, calculates degrees of separation, detects mutual friends, and identifies the top 5 most connected users
